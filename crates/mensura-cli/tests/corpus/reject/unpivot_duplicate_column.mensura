// Reject (resolve): the new name column `ts` collides with the index column
// of the same name, which would break the no-duplicate-columns invariant.
unit Reading { ts: int }
store readings { unit { Reading } attr { temperature: real } attr { peak: real } }
view bad {
  readings |> unpivot ts reading
}
