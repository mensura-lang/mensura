// Reject (resolve): pivot's name and value columns must differ.
enum Metric { "lo", "hi" }
unit Obs { ts: int }
store obs { unit { Obs } var { metric: Metric } var { reading: real } }
view bad {
  obs |> pivot metric metric
}
