// Reject (resolve): shrink_key over an incomplete store with no establish step.
unit Reading { ts: int }
store readings { unit { Reading } attr { machine: string } }
view bad {
  readings |> extend_key machine |> shrink_key machine
}
