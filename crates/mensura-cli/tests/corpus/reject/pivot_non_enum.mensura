// Reject (resolve): `pivot`'s name column must be a finite-enumerable enum;
// `machine` is a plain string index column after its promotion.
unit Reading { ts: int }
store readings { unit { Reading } attr { machine: string } attr { temperature: real } }
view bad {
  readings |> promote machine |> pivot machine temperature
}
