// A `?` marks an optional value (it may be missing in an observed row);
// an attribute may be optional, a bare type stays total (ADR 0010).
unit Machine {
  id: string
}

store readings {
  unit { Machine }
  attr { commissioned: date }
  attr { last_service: date? }
  attr { vibration: real }
}
