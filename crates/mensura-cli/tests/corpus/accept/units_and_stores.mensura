// Basic units, a named enum, and a store over primitive attributes.
unit Machine {
  id: string
}

enum MachineStatus { "operational", "failure" }

store machines {
  unit { Machine }
  attr { commissioned: date }
  attr { status: MachineStatus }
}
