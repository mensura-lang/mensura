// A store that claims a concrete-unit shape and satisfies it.
unit Person {
  id: string
}

shape PersonRecord {
  unit { Person }
  attr { admission: date }
}

store students : PersonRecord {
  unit { Person }
  attr { admission: date }
}
