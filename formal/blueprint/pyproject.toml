[project]
name = "mensura-blueprint"
version = "0.1.0"
description = "Tooling for the blueprint of the Mensura formalization (ADR 0021)"
requires-python = ">=3.10"
dependencies = [
    "leanblueprint>=0.0.16",
]

[tool.uv]
package = false
