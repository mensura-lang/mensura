unit Machine {
  id: string
}

enum MachineStatus { "operational", "degraded", "failure" }

store machines {
  unit { Machine }
  const { commissioned: date }
  var   { status: MachineStatus }
  var   { operating_hours: real }
  var   { service_count: int }
  var   { online: bool }
}
