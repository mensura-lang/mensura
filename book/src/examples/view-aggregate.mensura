unit Reading {
  machine: string
  ts: int
}
store readings {
  unit { Reading }
  attr { kelvin: real }
}
view hottest {
  readings
  |> shrink_key ts
  |> assume { complete }
  |> group_map |k, g| (.max_kelvin = max g.kelvin)
}
