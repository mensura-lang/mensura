// A minimal Mensura program: one unit, one store.
// Every word here is an ordinary identifier; the leading `unit` and `store`
// are recognised by position, not because they are reserved keywords.

unit Machine {
  id: string
}

store machines {
  unit { Machine }
  attr {
    commissioned: date
    label: string
  }
}
