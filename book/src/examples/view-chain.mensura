import bag

unit Machine {
  id: string
}
store readings {
  unit { Machine }
  attr* { kelvin: real }
}
view celsius {
  readings |> flat_map |k, r| (.celsius = r.kelvin - 273.15)
}
view hottest_celsius {
  celsius
  |> assume { complete }
  |> map_bags |k, b| (.max_celsius = bag.max b.celsius)
}
